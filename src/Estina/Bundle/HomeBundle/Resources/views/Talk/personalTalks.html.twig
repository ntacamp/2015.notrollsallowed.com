<h2>Mano pranešimai</h2>


<ul>
{% for talk in talks %}
<li>
    <a href="{{ url('talk', {id: talk.id}) }}">{{ talk.title }}</a> ({{ talk.track }})
    <a href="{{ url('talk_edit', {id: talk.id}) }}">Redaguoti</a>
</li>
{% endfor %}
</ul>

<a href="{{ url('talk_new') }}">Naujas pranešimas</a>
