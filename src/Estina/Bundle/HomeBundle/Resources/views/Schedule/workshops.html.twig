{% extends "EstinaHomeBundle::page.html.twig" %}

{% block content %}
    <h1>Dirbtuvės</h1>

    <div class="schedule">
        <div class="container">
            <div class="row">
                {#
                {% for track in tracks %}
                    <div class="col-md-3 track">
                        {% if track.talks %}
                            {% for talk in track.talks %}
                                <div class="events">
                                    <img src="{{ talk.user.email | gravatar }}" class="avatar">

                                    <div class="events-body">
                                        <h4>{{ talk.user.name }}</h4>

                                        <p>{{ talk.title }}</p>
                                    </div>
                                </div>
                            {% endfor %}
                        {% endif %}
                    </div>
                {% else %}
                #}
                    <p>Paskelbtų dirbtuvių dar nėra. Bet jau greitai!</p>
                {# {% endfor %} #}
            </div>
        </div>
    </div>


{% endblock %}
